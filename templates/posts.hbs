{{#* inline "inside"}}
<table id="post-table">
    {{#each posts}}
    <tr>
       <th class="title"><a href="/post/{{slug}}">{{title}}</a></th>
       <th class="uuid">{{uuid}}</th>
    </tr>
    {{/each}}
</table>
{{/inline}}
{{> base}}
